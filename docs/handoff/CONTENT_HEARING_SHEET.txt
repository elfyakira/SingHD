============================================================
Singホールディングス サイトリニューアル
文言ヒアリングシート
============================================================

このシートは、サイトの各セクションに表示する文言を確定するためのものです。
各項目について、表示したい文言をご記入ください。

HANDOFF記載の内容を基本としつつ、実際に表示する文言として
最終確定をお願いします。


============================================================
1. 共通要素
============================================================

【ロゴ】
{LOGO}
→ ロゴテキスト表記（例: SING HOLDINGS / Sing Holdings / SING.）
→ 回答:

{LOGO_ICON}
→ 固定CTAボタン内のロゴアイコン（短縮表記）
→ 回答:


============================================================
2. ヘッダーナビゲーション
============================================================

{NAV_MIRAIKU}
→ ミライクページへのリンクテキスト（例: MIRAIKU / ミライク）
→ 回答:

{NAV_COMPANY}
→ 会社概要ページへのリンクテキスト（例: COMPANY / 会社概要）
→ 回答:

{NAV_PROJECT}
→ グループ会社ページへのリンクテキスト（例: PROJECT / グループ会社）
→ 回答:

{NAV_NEWS}
→ ニュースページへのリンクテキスト（例: NEWS / ニュース）
→ 回答:

{CTA_BUTTON}
→ モバイルメニュー内のCTAボタンテキスト（例: 無料相談を予約する）
→ 回答:


============================================================
3. フッター
============================================================

{FOOTER_COMPANY_DESCRIPTION}
→ フッターに表示する会社説明文
→ 回答:

{FOOTER_SERVICE_LABEL}
→ サービスカテゴリのラベル（例: SERVICE）
→ 回答:

{FOOTER_MIRAIKU}
→ ミライクのリンクテキスト
→ 回答:

{FOOTER_CORPORATE_LABEL}
→ 企業情報カテゴリのラベル（例: CORPORATE）
→ 回答:

{FOOTER_COMPANY}
→ 会社概要のリンクテキスト
→ 回答:

{FOOTER_PROJECT}
→ グループ会社のリンクテキスト
→ 回答:

{FOOTER_NEWS}
→ ニュースのリンクテキスト
→ 回答:

{FOOTER_CONTACT}
→ お問い合わせのリンクテキスト
→ 回答:

{FOOTER_LEGAL_LABEL}
→ 法的情報カテゴリのラベル（例: LEGAL）
→ 回答:

{FOOTER_PRIVACY}
→ プライバシーポリシーのリンクテキスト
→ 回答:

{SNS_ICONS}
→ 表示するSNS（例: X / Instagram / Facebook など）
→ 回答:

{FOOTER_COPYRIGHT}
→ コピーライト表記（例: © Sing Holdings Inc. All Rights Reserved.）
→ 回答:


============================================================
4. TOPページ - ヒーローセクション
============================================================

{HERO_MAIN_COPY}
→ メインキャッチコピー
→ HANDOFF案: 夢を、ビジネスに。
→ 回答:

{HERO_SUB_COPY}
→ サブキャッチコピー
→ HANDOFF案: 起業という選択を、もっと現実的に。
→ 回答:

{HERO_CTA_BUTTON}
→ CTAボタンテキスト
→ HANDOFF案: 無料相談を予約する
→ 回答:

{HERO_IMAGE}
→ ヒーロー画像の説明（撮影指示用）
→ 回答:


============================================================
5. TOPページ - ミライク概要セクション
============================================================

{MIRAIKU_LABEL}
→ セクションラベル（例: SERVICE / 起業支援）
→ 回答:

{MIRAIKU_TITLE_EN}
→ 英語タイトル（例: MIRAIKU）
→ 回答:

{MIRAIKU_DESCRIPTION}
→ ミライクの説明文
→ 回答:

{MIRAIKU_LINK_TEXT}
→ 詳細ページへのリンクテキスト（例: 詳細を見る）
→ 回答:

{MIRAIKU_IMAGE}
→ ミライク紹介画像の説明（撮影指示用）
→ 回答:


============================================================
6. TOPページ - 選ばれる理由セクション
============================================================

{REASONS_LABEL}
→ セクションラベル（例: WHY US / 選ばれる理由）
→ 回答:

{REASONS_TITLE}
→ セクションタイトル
→ 回答:

【理由1】
{REASON_1_TITLE}
→ HANDOFF案: 若い代表によるリアルな挑戦環境
→ 回答:

{REASON_1_DESCRIPTION}
→ 説明文
→ 回答:

【理由2】
{REASON_2_TITLE}
→ HANDOFF案: 実行基盤が整っている
→ 回答:

{REASON_2_DESCRIPTION}
→ 説明文
→ 回答:

【理由3】
{REASON_3_TITLE}
→ HANDOFF案: 伴走型支援
→ 回答:

{REASON_3_DESCRIPTION}
→ 説明文
→ 回答:


============================================================
7. TOPページ - 支援の流れセクション
============================================================

{FLOW_LABEL}
→ セクションラベル（例: FLOW / 支援の流れ）
→ 回答:

{FLOW_TITLE}
→ セクションタイトル
→ 回答:

【ステップ1】
{FLOW_STEP_1_TITLE}
→ HANDOFF案: 無料相談
→ 回答:

{FLOW_STEP_1_DESCRIPTION}
→ HANDOFF案: まずは話を聞く
→ 回答:

【ステップ2】
{FLOW_STEP_2_TITLE}
→ HANDOFF案: 事業設計・市場検証
→ 回答:

{FLOW_STEP_2_DESCRIPTION}
→ HANDOFF案: アイデアを形にする
→ 回答:

【ステップ3】
{FLOW_STEP_3_TITLE}
→ HANDOFF案: 収益モデル構築
→ 回答:

{FLOW_STEP_3_DESCRIPTION}
→ HANDOFF案: 稼ぐ仕組みを作る
→ 回答:

【ステップ4】
{FLOW_STEP_4_TITLE}
→ HANDOFF案: 実行・伴走支援
→ 回答:

{FLOW_STEP_4_DESCRIPTION}
→ HANDOFF案: 一緒に走る
→ 回答:


============================================================
8. TOPページ - 代表メッセージセクション
============================================================

{MESSAGE_LABEL}
→ セクションラベル（例: MESSAGE / 代表メッセージ）
→ 回答:

{MESSAGE_TITLE}
→ セクションタイトル
→ 回答:

{MESSAGE_BODY}
→ メッセージ本文
→ HANDOFF案: 起業は才能ではなく、環境で決まる。挑戦する人が孤立しない社会をつくりたい。
→ 回答:

{MESSAGE_POSITION}
→ 代表の肩書（例: 代表取締役）
→ 回答:

{MESSAGE_NAME}
→ 代表の氏名
→ 回答:

{MESSAGE_IMAGE}
→ 代表写真の説明（撮影指示用）
→ 回答:


============================================================
9. TOPページ - 無料相談CTAセクション
============================================================

{CTA_TITLE}
→ セクションタイトル
→ HANDOFF案: まずは話を聞いてみませんか？
→ 回答:

{CTA_SUBTITLE}
→ サブテキスト
→ 回答:

{CTA_BUTTON}
→ CTAボタンテキスト
→ HANDOFF案: 無料相談を予約する
→ 回答:

{CTA_INFO_1}
→ 補足情報1
→ HANDOFF案: オンライン・対面対応
→ 回答:

{CTA_INFO_2}
→ 補足情報2
→ HANDOFF案: 60分
→ 回答:

{CTA_INFO_3}
→ 補足情報3
→ HANDOFF案: 代表が直接対応
→ 回答:


============================================================
10. 固定CTAボタン
============================================================

{FIXED_CTA_TEXT}
→ 右下固定CTAボタンのテキスト（例: 無料相談はこちら）
→ 回答:


============================================================
11. ミライク詳細ページ - ヒーロー
============================================================

{MIRAIKU_HERO_SUBTITLE}
→ ヒーローのサブタイトル（例: 起業支援プログラム）
→ 回答:


============================================================
12. ミライク詳細ページ - サービス概要
============================================================

{MIRAIKU_OVERVIEW_LABEL}
→ セクションラベル（例: ABOUT / サービス概要）
→ 回答:

{MIRAIKU_OVERVIEW_TITLE}
→ セクションタイトル（例: ミライクとは）
→ 回答:

{MIRAIKU_OVERVIEW_DESCRIPTION}
→ ミライクの詳細説明文
→ 回答:

{MIRAIKU_TARGET_TITLE}
→ 対象者セクションのタイトル（例: こんな方におすすめ）
→ 回答:

{MIRAIKU_TARGET_1}
→ 対象者1（例: 22〜35歳の起業志望者）
→ 回答:

{MIRAIKU_TARGET_2}
→ 対象者2（例: 本気で挑戦したい方）
→ 回答:

{MIRAIKU_TARGET_3}
→ 対象者3（例: 半年以内に動きたい方）
→ 回答:


============================================================
13. ミライク詳細ページ - 支援内容詳細（6つ）
============================================================

{SUPPORT_LABEL}
→ セクションラベル（例: SUPPORT / 支援内容）
→ 回答:

{SUPPORT_TITLE}
→ セクションタイトル
→ 回答:

{SUPPORT_DESCRIPTION}
→ 支援内容セクションの導入文
→ 回答:

【支援1】
{SUPPORT_1_TITLE}
→ HANDOFF案: 事業設計
→ 回答:

{SUPPORT_1_DESCRIPTION}
→ 支援1の説明文
→ 回答:

【支援2】
{SUPPORT_2_TITLE}
→ HANDOFF案: 収益モデル構築
→ 回答:

{SUPPORT_2_DESCRIPTION}
→ 支援2の説明文
→ 回答:

【支援3】
{SUPPORT_3_TITLE}
→ HANDOFF案: 法人設立支援
→ 回答:

{SUPPORT_3_DESCRIPTION}
→ 支援3の説明文
→ 回答:

【支援4】
{SUPPORT_4_TITLE}
→ HANDOFF案: 営業支援
→ 回答:

{SUPPORT_4_DESCRIPTION}
→ 支援4の説明文
→ 回答:

【支援5】
{SUPPORT_5_TITLE}
→ HANDOFF案: マーケティング支援
→ 回答:

{SUPPORT_5_DESCRIPTION}
→ 支援5の説明文
→ 回答:

【支援6】
{SUPPORT_6_TITLE}
→ HANDOFF案: 資金計画サポート
→ 回答:

{SUPPORT_6_DESCRIPTION}
→ 支援6の説明文
→ 回答:


============================================================
14. ミライク詳細ページ - 支援の流れ詳細
============================================================

{FLOW_DETAIL_LABEL}
→ セクションラベル（例: FLOW / 支援の流れ）
→ 回答:

{FLOW_DETAIL_TITLE}
→ セクションタイトル
→ 回答:

【ステップ1】
{FLOW_DETAIL_STEP_1_TITLE}
→ HANDOFF案: 無料相談
→ 回答:

{FLOW_DETAIL_STEP_1_DESCRIPTION}
→ 詳細説明文（例: まずはお気軽にご相談ください。現在の状況や目標をお聞かせいただき、最適な支援プランをご提案します。）
→ 回答:

【ステップ2】
{FLOW_DETAIL_STEP_2_TITLE}
→ HANDOFF案: 事業設計・市場検証
→ 回答:

{FLOW_DETAIL_STEP_2_DESCRIPTION}
→ 詳細説明文
→ 回答:

【ステップ3】
{FLOW_DETAIL_STEP_3_TITLE}
→ HANDOFF案: 収益モデル構築
→ 回答:

{FLOW_DETAIL_STEP_3_DESCRIPTION}
→ 詳細説明文
→ 回答:

【ステップ4】
{FLOW_DETAIL_STEP_4_TITLE}
→ HANDOFF案: 実行・伴走支援
→ 回答:

{FLOW_DETAIL_STEP_4_DESCRIPTION}
→ 詳細説明文
→ 回答:


============================================================
15. ミライク詳細ページ - グループ参画
============================================================

{GROUP_LABEL}
→ セクションラベル（例: JOIN US / グループ参画）
→ 回答:

{GROUP_TITLE}
→ セクションタイトル（例: グループ会社への参画）
→ 回答:

{GROUP_DESCRIPTION}
→ グループ参画の説明文
→ 回答:

{GROUP_TYPE_TITLE}
→ 参画形態タイトル（例: 参画形態）
→ 回答:

{GROUP_TYPE_1}
→ HANDOFF案: 新規事業立ち上げ
→ 回答:

{GROUP_TYPE_2}
→ HANDOFF案: 経営幹部候補
→ 回答:

{GROUP_TYPE_3}
→ HANDOFF案: 事業責任者
→ 回答:

{GROUP_MERIT_TITLE}
→ メリットタイトル（例: 参画メリット）
→ 回答:

【メリット1】
{GROUP_MERIT_1_TITLE}
→ HANDOFF案: 給与保証
→ 回答:

{GROUP_MERIT_1_DESC}
→ メリット1の説明
→ 回答:

【メリット2】
{GROUP_MERIT_2_TITLE}
→ HANDOFF案: 資金バックアップ
→ 回答:

{GROUP_MERIT_2_DESC}
→ メリット2の説明
→ 回答:

【メリット3】
{GROUP_MERIT_3_TITLE}
→ HANDOFF案: 営業基盤共有
→ 回答:

{GROUP_MERIT_3_DESC}
→ メリット3の説明
→ 回答:

【メリット4】
{GROUP_MERIT_4_TITLE}
→ HANDOFF案: 経営ノウハウ提供
→ 回答:

{GROUP_MERIT_4_DESC}
→ メリット4の説明
→ 回答:


============================================================
16. ミライク詳細ページ - FAQ
============================================================

{FAQ_LABEL}
→ セクションラベル（例: FAQ / よくある質問）
→ 回答:

{FAQ_TITLE}
→ セクションタイトル
→ 回答:

【Q1】
{FAQ_Q1}
→ HANDOFF案: 未経験でも可能ですか？
→ 回答:

{FAQ_A1}
→ HANDOFF案: 可能ですが本気度は必要です。
→ 回答:

【Q2】
{FAQ_Q2}
→ HANDOFF案: 資金がありません
→ 回答:

{FAQ_A2}
→ HANDOFF案: モデル設計から支援します。
→ 回答:

【Q3】
{FAQ_Q3}
→ HANDOFF案: 学生でも可能？
→ 回答:

{FAQ_A3}
→ HANDOFF案: 可能です。
→ 回答:

【Q4】
{FAQ_Q4}
→ HANDOFF案: 収益化までどれくらい？
→ 回答:

{FAQ_A4}
→ HANDOFF案: 事業により異なります。
→ 回答:

【Q5】
{FAQ_Q5}
→ HANDOFF案: 途中解約は可能？
→ 回答:

{FAQ_A5}
→ HANDOFF案: 契約条件を明示します。
→ 回答:


============================================================
17. ミライク詳細ページ - CTA
============================================================

{MIRAIKU_CTA_TITLE}
→ CTAタイトル（例: まずは無料相談から）
→ 回答:

{MIRAIKU_CTA_SUBTITLE}
→ CTAサブテキスト
→ 回答:

{MIRAIKU_CTA_BUTTON}
→ CTAボタンテキスト（例: 無料相談を予約する）
→ 回答:

{MIRAIKU_CTA_INFO_1}
→ 補足情報1（例: オンライン・対面対応）
→ 回答:

{MIRAIKU_CTA_INFO_2}
→ 補足情報2（例: 60分）
→ 回答:

{MIRAIKU_CTA_INFO_3}
→ 補足情報3（例: 代表が直接対応）
→ 回答:


============================================================
備考欄
============================================================

その他、文言に関するご要望やご質問があればこちらにご記入ください。

→

============================================================
